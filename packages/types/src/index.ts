// @anio/types — Shared domain types for Anio Regalia OS
// This package will hold the canonical type system.
// Currently, types live in apps/web/lib/types.ts and are re-exported here
// for future consumers (convex functions, packages, etc.)

// TODO: Move the full type system here during the extraction refactor.
// For now this is a placeholder to establish the package in the workspace.

export {}
